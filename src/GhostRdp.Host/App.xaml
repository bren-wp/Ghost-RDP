<Application x:Class="GhostRdp.Host.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:GhostRdp.Host">
    <Application.Resources>
        <SolidColorBrush x:Key="WindowBrush" Color="#0B0F17" />
        <SolidColorBrush x:Key="PanelBrush" Color="#121824" />
        <SolidColorBrush x:Key="PanelAltBrush" Color="#161D2A" />
        <SolidColorBrush x:Key="BorderBrush" Color="#2C3648" />
        <SolidColorBrush x:Key="TextBrush" Color="#F2F5FA" />
        <SolidColorBrush x:Key="MutedBrush" Color="#97A3B8" />
        <SolidColorBrush x:Key="AccentBrush" Color="#5B7CFA" />
        <SolidColorBrush x:Key="AccentStrongBrush" Color="#7A98FF" />
        <SolidColorBrush x:Key="SuccessBrush" Color="#4AD79B" />
        <SolidColorBrush x:Key="WarningBrush" Color="#F2BA55" />
        <SolidColorBrush x:Key="DangerBrush" Color="#FF6878" />
        <SolidColorBrush x:Key="SelectionBrush" Color="#202F50" />

        <DrawingImage x:Key="GhostRdpWindowIcon">
            <DrawingImage.Drawing>
                <DrawingGroup>
                    <GeometryDrawing Brush="#5B7CFA" Geometry="M2,3 H22 V18 H14 V20 H18 V22 H6 V20 H10 V18 H2 Z" />
                    <GeometryDrawing Brush="#0B0F17" Geometry="M5,6 H19 V15 H5 Z" />
                    <GeometryDrawing Brush="#7A98FF" Geometry="M8,9 C8,7.9 8.9,7 10,7 H14 C15.1,7 16,7.9 16,9 V13 H14.5 V10.5 H9.5 V13 H8 Z" />
                </DrawingGroup>
            </DrawingImage.Drawing>
        </DrawingImage>

        <Style x:Key="AppWindowStyle" TargetType="Window">
            <Setter Property="Background" Value="{DynamicResource WindowBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
            <Setter Property="FontFamily" Value="Segoe UI Variable Text, Segoe UI" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Icon" Value="{StaticResource GhostRdpWindowIcon}" />
            <Setter Property="UseLayoutRounding" Value="True" />
            <Setter Property="SnapsToDevicePixels" Value="True" />
            <Setter Property="TextOptions.TextFormattingMode" Value="Display" />
        </Style>
        <Style TargetType="{x:Type local:MainWindow}" BasedOn="{StaticResource AppWindowStyle}" />

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
            <Setter Property="TextOptions.TextFormattingMode" Value="Display" />
        </Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource AccentBrush}" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="16,10" />
            <Setter Property="MinHeight" Value="40" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Chrome" Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" RecognizesAccessKey="True" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Chrome" Property="Background" Value="{DynamicResource AccentStrongBrush}" />
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="Chrome" Property="BorderBrush" Value="{DynamicResource TextBrush}" />
                                <Setter TargetName="Chrome" Property="BorderThickness" Value="2" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Chrome" Property="Opacity" Value="0.45" />
                                <Setter Property="Cursor" Value="Arrow" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ValueTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="TextWrapping" Value="Wrap" />
        </Style>

        <Style x:Key="LabelTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource MutedBrush}" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="Margin" Value="0,0,0,4" />
        </Style>
    </Application.Resources>
</Application>
